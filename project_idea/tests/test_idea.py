from odoo.tests import common

class TestProjectIdea(common.TransactionCase):
    def test_name(self):
        #record = self.env['model.a'].create({'field': 'value'})
        #record.some_action()
        self.assertEqual(5,5)
        self.assertEqual(5,8)